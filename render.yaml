services:
  # BookInfoFinder Web App
  - type: web
    name: bookinfo-app
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: EMAIL_ADDRESS
        value: haivvde180313@fpt.edu.vn
      - key: EMAIL_PASSWORD
        sync: false # Will be set manually in Render dashboard
      - key: GEMINI_API_KEY
        sync: false # Will be set manually in Render dashboard
    # Note: Database connection will be added after PostgreSQL service is created